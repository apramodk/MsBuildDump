# MsBuildDump
A CLI tool that extracts project structure, source files, and dependencies from Visual Studio solutions (.sln) into JSON, bridging Windows/MSBuild codebases to code indexing and RAG pipelines.
